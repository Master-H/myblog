import React, {
  SFC,
} from 'react';
import Header from '../header';

// interface MyProps {
//   onClick: (param: number) => void;
//   age: number;
// }
const Home: SFC<{}> = () => (
  <div>
    <Header />
  </div>
);
export default Home;
